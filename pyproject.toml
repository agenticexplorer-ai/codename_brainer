[project]
name = "agentkit"
version = "0.1.0"
description = "A portable multi-agent dev orchestrator (personas + workflows + tools)."
requires-python = ">=3.10"
dependencies = ["pyyaml"]

[project.scripts]
agentkit = "agentkit.runner.cli:main"